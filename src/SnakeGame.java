public class SnakeGame {

    // entry point
    public static void main(String[] args)  {
        /**
         * Instantiating GameFrame object and is equivalent to:
         * GameFrame frame = new GameFrame();        
         * Omitting explicitly defining frame as we will not call methods on it.
         * Constructor is called and this is all we use.
         */
      
        new GameFrame(); 
    }
}
